import styled from "styled-components";

export const StyleSlider = styled.div`
  overflow: hidden;
  padding-bottom: 100px;
  margin-bottom: -100px;
  .swiper {
    overflow: visible;
  }
  .swiper-pagination {
    bottom: -50px;
  }
`;
